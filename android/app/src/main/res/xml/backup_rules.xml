<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Hope Cards uses its explicit in-app backup and restore flow. -->
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
</full-backup-content>
